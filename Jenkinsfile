pipeline {
    agent any
  
    stages {
        stage('Build') {
            steps {
                sh 'echo "executing shell script"'
            }
        }
    }
    node {
      echo "hi buddy!"
    }
}
